# Financial-news-sentiment-analyzer
Built a Python pipeline that pulls recent news per ticker, computes VADER sentiment per headline, aggregates daily sentiment into time series, and exposes CSVs and a Streamlit demo — a reproducible baseline to convert text into trading signals and prototype FinNLP ideas quickly.
